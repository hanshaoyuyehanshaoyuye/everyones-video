# everyones-video 轻量核心依赖
# pip install -r requirements.txt     ← 装这个就行，几十 MB，无模型下载
# pip install -r requirements-full.txt ← 需要本地离线 ASR 时再装这个

# 核心：YouTube 字幕 + 翻译 + 配音 + 烧录
yt-dlp>=2026.1.1
edge-tts>=6.0.0
rapidfuzz>=3.0.0

# 系统工具（非 pip 安装）：
#   ffmpeg（--burn 烧录字幕用）
#   bash >= 4.0（pipeline.sh 运行环境）
